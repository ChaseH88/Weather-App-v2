export enum Units {
    imperial = 'imperial',
    metric = 'metric'
  }
  
export enum Temperatures {
fahrenheit = 'fahrenheit',
celsius = 'celsius'
}

export enum AppKeys {
  search_history = 'search_history',
  coordinates = 'coordinates',
  dark_mode = 'dark_mode'
}